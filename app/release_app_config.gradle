// Following config will be merged with the default one
// for "release" builds.
ext {
    enable_analytics = true
    logs_enabled = false
    available_envs = ['production', 'demo']
    default_env_id = 'production'
}