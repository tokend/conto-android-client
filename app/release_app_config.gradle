// Following config will be merged with the default one
// for "release" builds.
ext {
    enable_analytics = true
    logs_enabled = false
    available_envs = ['forklog']
    default_env_id = 'forklog'
    app_host = 'forklog.conto.me'
}